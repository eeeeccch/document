```bash
# 버전 확인
node -v

# 강제 캐시
sudo npm cache clean -f

# n모듈 설치
sudo npm install -g n

# n모듈 활용 nodejs 설치
# sudo n version(12.18.1)
sudo n stable

# npm 버전 확인
npm -v

# npm으로 npm 업데이트
sudo npm install -g npm  
```
